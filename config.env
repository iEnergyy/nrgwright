# Grafana Cloud Configuration
# Replace these with your actual Grafana Cloud credentials
GRAFANA_CLOUD_URL=https://ienergyy.grafana.net
GRAFANA_CLOUD_SERVICE_ACCOUNT_TOKEN=tokennn

# Metrics Server Configuration
METRICS_PORT=9090
METRICS_HOST=localhost

# Optional: Prometheus remote write URL (if using direct push)
PROMETHEUS_REMOTE_WRITE_URL=https://ienergyy.grafana.net/api/v1/push
PROMETHEUS_TOKEN=tookennnn